T.C: O(N), S.C: O(1)

  static int[] OptimalprefixSum(int arr[]){
	 int Pf[] = new int[arr.length];         // creating array of same size of arr to store prefix sum ; remember why [] because putting inplace of size
     Pf[0]=arr[0];
	 for(int i=1;i<arr.length;i++){
       Pf[i] = Pf[i-1]+arr[i];
	}
	return Pf;
  }

	


